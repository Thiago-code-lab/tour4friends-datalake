# docker-compose.yml
version: "3.8"
services:
  mongodb:
    image: mongo:7.0
    container_name: tour4friends_mongo
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: tour4friends
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data: